const heading = React.createElement('h1',{},'Hello world from react');
const root = ReactDOM.createRoot(document.querySelector('#root'));              //Root is Top most DOM file
root.render(heading);